# Исправления для Проблемы с Базой Данных

## Проблема

База данных Supabase переполнена на **265% (1.3 GB / 0.5 GB)** из-за:
1. Дубликатов (27% данных)
2. Отсутствия автоматической очистки
3. Множественных запусков cron job

## Применённые Исправления

### 1. ✅ Защита от Параллельных Запусков Cron Job

**Файлы:**
- `lib/cron-lock.ts` (новый) - механизм блокировки
- `app/api/cron/update-funding/route.ts` (изменён)

**Что сделано:**
- Добавлена in-memory блокировка для предотвращения параллельных выполнений
- Если cron job уже выполняется, повторный вызов вернёт 409 Conflict
- Блокировка автоматически снимается через 5 минут (защита от зависания)

**Результат:** Дубликаты больше не будут создаваться, даже если Vercel запустит cron несколько раз.

### 2. ✅ Скрипты для Мониторинга и Очистки

**Созданные файлы:**
- `scripts/check-database-size.js` - анализ размера БД
- `scripts/analyze-data-growth.js` - анализ роста данных
- `scripts/find-duplicates.js` - поиск дубликатов
- `scripts/cleanup-old-data.js` - скрипт очистки старых данных
- `scripts/README.md` - документация

**Использование:**
```bash
# Проверить размер БД
node scripts/check-database-size.js

# Найти дубликаты
node scripts/find-duplicates.js

# Удалить данные старше 7 дней (preview)
node scripts/cleanup-old-data.js --dry-run --days=7

# Реально удалить
node scripts/cleanup-old-data.js --days=7
```

### 3. ✅ SQL Миграции для Очистки

**Файлы:**
- `supabase/migrations/cleanup_old_data.sql` - разовая очистка
- `supabase/migrations/add_automatic_cleanup.sql` - функция автоочистки

**Использование:**
```bash
# Запустить через скрипт
node scripts/run-migration.js cleanup_old_data.sql

# Или вручную в Supabase Dashboard → SQL Editor
```

### 4. ✅ Документация

**Файлы:**
- `DATABASE_CLEANUP_GUIDE.md` - полный гайд по очистке
- `scripts/README.md` - документация скриптов
- `FIXES_APPLIED.md` (этот файл)

---

## Что Делать Дальше

### Шаг 1: Очистить Существующие Данные (СРОЧНО!)

Зайди в Supabase Dashboard → SQL Editor:
https://supabase.com/dashboard/project/_/sql/new

Скопируй и запусти:
```sql
-- Удалит данные старше 3 дней (~780k записей)
DELETE FROM funding_rates WHERE created_at < NOW() - INTERVAL '3 days';
DELETE FROM funding_spreads WHERE created_at < NOW() - INTERVAL '3 days';
```

**Ожидаемый результат:**
- База уменьшится с 308 MB до ~225 MB
- Освободится ~27% места
- Уйдёте из критической зоны

### Шаг 2: Настроить Автоматическую Очистку

В том же SQL Editor создайте функцию очистки:
```sql
CREATE OR REPLACE FUNCTION cleanup_old_funding_data()
RETURNS void LANGUAGE plpgsql SECURITY DEFINER
AS $$
BEGIN
  DELETE FROM funding_rates WHERE created_at < NOW() - INTERVAL '7 days';
  DELETE FROM funding_spreads WHERE created_at < NOW() - INTERVAL '7 days';
  RAISE NOTICE 'Cleaned up funding data older than 7 days';
END;
$$;
```

Затем вызывайте её **раз в неделю** (вручную или через отдельный cron):
```sql
SELECT cleanup_old_funding_data();
```

### Шаг 3: Задеплоить Исправления

Код уже изменён локально. Теперь нужно задеплоить:

```bash
# Коммит изменений
git add .
git commit -m "Fix: Add cron lock to prevent duplicate executions and database scripts"
git push

# Vercel автоматически задеплоит
```

### Шаг 4: Проверить Vercel Cron Settings

1. Зайди в Vercel Dashboard → твой проект → Settings → Cron Jobs
2. Убедись, что есть **только одно** задание для `/api/cron/update-funding`
3. Проверь расписание: должно быть `*/5 * * * *` (каждые 5 минут)
4. Если задание "отключено по причине провалов", посмотри логи и перезапусти

---

## Мониторинг После Исправлений

После деплоя новой версии с защитой от дубликатов:

1. **Сразу после деплоя:**
   ```bash
   node scripts/find-duplicates.js
   ```
   Должно показать 0% дубликатов (или близко к нулю)

2. **Через 24 часа:**
   ```bash
   node scripts/check-database-size.js
   ```
   Рост должен быть ~200k записей/день (вместо 754k)

3. **Еженедельно:**
   - Запускай `SELECT cleanup_old_funding_data();` в Supabase
   - Проверяй размер БД

---

## Ожидаемые Результаты

### До исправлений:
- 📊 Размер БД: 308 MB (265% лимита)
- 📈 Рост: ~754k записей/день
- ⚠️ Дубликаты: 27.1%
- ⏳ Время до лимита: 7 дней

### После исправлений:
- 📊 Размер БД: ~225 MB (после первой очистки)
- 📈 Рост: ~200k записей/день (без дубликатов)
- ✅ Дубликаты: 0%
- ⏳ Стабильно под 500 MB с еженедельной очисткой

---

## Контрольный Чек-лист

- [ ] Запустить SQL очистку в Supabase Dashboard
- [ ] Создать функцию `cleanup_old_funding_data()` в Supabase
- [ ] Задеплоить код с защитой от дубликатов
- [ ] Проверить Vercel Cron настройки (только 1 задание)
- [ ] Через 1 час: проверить дубликаты (`node scripts/find-duplicates.js`)
- [ ] Через 24 часа: проверить рост БД (`node scripts/check-database-size.js`)
- [ ] Настроить еженедельное напоминание запускать очистку

---

## Помощь

Если что-то не работает:

1. **Дубликаты всё ещё появляются?**
   - Проверь логи Vercel, возможно несколько deployments запущены
   - Проверь что новая версия с `cron-lock.ts` задеплоена

2. **База всё равно растёт быстро?**
   - Запусти `node scripts/analyze-data-growth.js`
   - Проверь количество отслеживаемых символов

3. **Timeout при очистке?**
   - Используй SQL в Supabase Dashboard, он не имеет таймаутов
   - Можно удалять порциями: `WHERE created_at < NOW() - INTERVAL '30 days'` сначала

4. **Всё равно нужна помощь?**
   - Отправь вывод `node scripts/check-database-size.js`
   - Приложи скриншот Vercel Cron настроек
